#include<stdio.h>
main()
{
	int sum, num, cnt,a;

	sum = 0;
	num = 0;
	cnt = 0;
	a = 0;
	printf("®”:%d",a);

	while ((scanf("%d", &a)) != EOF) {
		sum += a;
		cnt++;
		printf("®”:%d", a);
	}
	num = sum / cnt;
	printf("‡Œv = % d •½‹Ï = % .2f", sum, (float)num);
}